# Status Share Delivery

The ordered share workflow is `status_shared` -> `status_share_opened`, linked by `share_id`.

## Questions the PM will ask

- What share of status_shared events reach status_share_opened?
- How does opening vary by channel and recipient_type?
