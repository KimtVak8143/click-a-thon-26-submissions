# Recovery Benchmark

The ordered workflow is `checkout_abandoned` -> `checkout_recovered`, linked by
`application_id`.

## Questions the PM will ask

- What share of checkout_abandoned events reach checkout_recovered?
- How does our recovery rate compare with a competitor dataset that is not supplied here?
