# Multi-Currency Forex Add-on

The ordered add-on workflow is `forex_addon_shown` -> `forex_addon_purchased`, linked by
`application_id`. The monetary `amount` is emitted with `currency`, and purchases can use
different currencies.

## Questions the PM will ask

- What share reaches forex_addon_purchased from forex_addon_shown?
- What is purchased monetary amount grouped by currency and destination?
